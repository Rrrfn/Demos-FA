# -*- coding: utf-8 -*-
"""پیکربندی حس‌سنج — یک منبع حقیقت برای مسیرها، برچسب‌ها و آستانه‌ها.

هر عددی که هم مدل و هم رابط به آن نیاز دارند اینجاست: حجم دیتاست، آستانه‌های
اطمینان، سقف ورودی، و بازهٔ زمانی روند. هیچ‌جای دیگری نباید آستانهٔ اطمینان یا
سقف طول متن از نو تعریف شود؛ وگرنه رابط و مدل می‌توانند از هم جدا بیفتند.
"""
from __future__ import annotations

import os

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR = os.path.join(BASE_DIR, "data")
MODELS_DIR = os.path.join(BASE_DIR, "models")
STATIC_DIR = os.path.join(BASE_DIR, "static")
TEMPLATES_DIR = os.path.join(BASE_DIR, "templates")

for _path in (DATA_DIR, MODELS_DIR):
    os.makedirs(_path, exist_ok=True)

# ------------------------------------------------------------------ تکرارپذیری
RANDOM_SEED = 42
TEST_SIZE = 0.2
CV_FOLDS = 5
N_PER_CLASS = 600          # ۶۰۰ مثبت + ۶۰۰ خنثی + ۶۰۰ منفی = ۱۸۰۰

# ------------------------------------------------------------------ برچسب‌ها
LABELS = ("pos", "neu", "neg")
LABEL_FA = {"pos": "مثبت", "neu": "خنثی", "neg": "منفی"}
LABEL_EMOJI = {"pos": "🙂", "neu": "😐", "neg": "🙁"}

# ------------------------------------------------------------------ آستانه‌ها
#: اطمینان بالای این عدد «قابل اتکا» شمرده می‌شود.
CONFIDENCE_HIGH = 0.70
#: بین این عدد و ``CONFIDENCE_HIGH`` یعنی «مرزی» — نتیجه گزارش می‌شود ولی با
#: هشدار. پایین‌تر از آن، تصمیم مدل «نامطمئن» اعلام می‌شود.
CONFIDENCE_LOW = 0.50
#: کمترین شمار توکن معنادار برای این‌که پیش‌بینی اصلاً ارزش گزارش داشته باشد.
#: زیر این عدد، بردار ویژگی تقریباً خالی است و مدل عملاً حدس می‌زند.
MIN_TOKENS = 1
#: متن بدون هیچ توکن شناخته‌شده — نتیجه باید «سیگنال کافی نیست» باشد، نه برچسب.
MIN_KNOWN_TOKENS = 1

# ------------------------------------------------------------------ سقف ورودی
#: سقف متن برای تحلیل تک‌متن. فراتر از این، برش می‌خورد و در رابط اعلام می‌شود.
MAX_ANALYZE_CHARS = 4000
#: سقف ردیف‌های تحلیل گروهی.
MAX_BATCH_ROWS = 5000
#: سقف حجم فایل بارگذاری‌شده (بایت).
MAX_UPLOAD_BYTES = 5 * 1024 * 1024
#: نام‌های محتمل ستون متن در فایل بارگذاری‌شده.
TEXT_COLUMN_NAMES = ("text", "comment", "review", "متن", "نظر", "کامنت", "دیدگاه")
#: نام‌های محتمل ستون تاریخ — برای نمودار روند.
DATE_COLUMN_NAMES = ("date", "created_at", "تاریخ", "زمان")

# ------------------------------------------------------------------ روند
#: شمار ماه‌های مصنوعی در دیتاست نمونه. روند *خودِ دیتاست* است، نه ادعایی
#: دربارهٔ بازار واقعی — در صفحهٔ متدولوژی صریح گفته شده.
TREND_MONTHS = 6
#: وزن ماهانهٔ **درون هر کلاس** — نه ترکیب کلاس‌ها در یک ماه.
#:
#: این تفکیک مهم است و یک بار اشتباه گرفته شد: با توازن دقیق کلاس‌ها (۶۰۰
#: نمونه در هر کلاس) امکان ندارد ترکیب ماهانه همزمان همان اعداد این جدول
#: باشد. پس این جدول می‌گوید هر کلاس چگونه میان شش ماه پخش می‌شود، و ترکیب
#: نهایی از تقسیم آن بر شمار ماهانهٔ همهٔ کلاس‌ها به دست می‌آید — همان کاری
#: که ``analytics.declared_trend`` می‌کند و آزمون روی آن است.
MIX_BY_MONTH = (
    {"pos": 0.34, "neu": 0.36, "neg": 0.30},
    {"pos": 0.36, "neu": 0.34, "neg": 0.30},
    {"pos": 0.39, "neu": 0.32, "neg": 0.29},
    {"pos": 0.42, "neu": 0.30, "neg": 0.28},
    {"pos": 0.44, "neu": 0.29, "neg": 0.27},
    {"pos": 0.47, "neu": 0.28, "neg": 0.25},
)

#: ستون‌های خروجی تحلیل گروهی.
BATCH_OUTPUT_COLUMNS = ("row", "text", "label", "label_fa", "confidence", "band",
                        "tokens", "top_signals")


def dataset_path() -> str:
    return os.path.join(DATA_DIR, "comments.csv")


def model_path() -> str:
    return os.path.join(MODELS_DIR, "sentiment_model.joblib")


def metrics_path() -> str:
    return os.path.join(MODELS_DIR, "metrics.json")

