# وابستگی‌های اجرا.
#
# این پروژه Flask است، نه Streamlit: نسخهٔ قبلی برای رسم نمودار یک بستهٔ
# JavaScript (Altair → Vega-Lite) و برای جدول‌ها Arrow را به مرورگر می‌فرستاد
# و همان باعث بار سنگین صفحهٔ تحلیل می‌شد. نمودارها الان سمت سرور به SVG
# تبدیل می‌شوند و مرورگر فقط HTML و CSS می‌گیرد.
Flask>=3.0
gunicorn>=21.2
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
# برای ساخت نسخه‌های کم‌حجم عکس (tools/make_thumbs.py). در زمان ساخت لازم است
# و در زمان اجرا فقط اگر نسخه‌ها از قبل ساخته نشده باشند.
Pillow>=10.0
streamlit>=1.30.0
