# -*- coding: utf-8 -*-
"""نقطهٔ ورود تولید.

    gunicorn --bind 0.0.0.0:$PORT --workers 2 --threads 4 wsgi:app

این فایل دو کار بیرون از مسیر درخواست انجام می‌دهد، چون هر دو یک‌بار برای هر
فرایند لازم‌اند و اگر در مسیر درخواست بیفتند، هزینه‌شان را *اولین بازدیدکننده*
می‌دهد:

**۱) گرم‌کردن مدل.** بارگذاری بستهٔ مدل با ``joblib`` حدود ۱٫۷ ثانیه طول
می‌کشد (رمزگشایی مدل‌های scikit-learn). اگر اینجا انجام شود، نخستین بازدید از
صفحهٔ ملک هم مثل بقیه سریع است؛ وگرنه یک کاربر بدشانس تنها کسی است که آن
۱٫۷ ثانیه را می‌دهد.

**۲) ساخت نسخه‌های کم‌حجم عکس.** اگر مرحلهٔ ساخت (``buildCommand``) این کار را
کرده باشد، اینجا هیچ کاری انجام نمی‌شود و فقط چند بررسی وجود فایل انجام
می‌گیرد. اگر نکرده باشد، عکس‌ها صفحه را سنگین می‌کنند — نه شکسته.

تعداد کارگر عمداً کم است: هر کارگر یک نسخهٔ کامل از مدل و کاتالوگ را در حافظه
دارد. روی پلن رایگان، دو کارگر با چهار رشته تعادل درست بین پاسخ‌دهی هم‌زمان و
مصرف حافظه است.
"""
from __future__ import annotations

from khoneyab.web import create_app

app = create_app()

try:                                        # pragma: no cover - مسیر استقرار
    from tools.make_thumbs import ensure_all

    ensure_all()
except Exception as error:                  # نبودِ Pillow نباید بالا آمدن را بخواباند
    print(f"ساخت نسخه‌های کم‌حجم انجام نشد: {error}")

try:                                        # pragma: no cover - مسیر استقرار
    from khoneyab import services

    # بارگذاری مدل و یک محاسبهٔ کامل توضیح: هر دو حافظهٔ میانی را پر می‌کنند،
    # پس درخواست نخستِ کاربر راه گرم را می‌رود نه راه سرد.
    services.bundle()
    services.explanation(2, 120, 3, 4, 3, 1, 1, 1)
except Exception as error:                  # نبودِ مدل نباید بالا آمدن را بخواباند
    print(f"گرم‌کردن مدل انجام نشد: {error}")
